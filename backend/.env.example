# ===========================================
# Admin Login — optional here; set on first visit at http://localhost:5174
# ===========================================
ADMIN_EMAIL=
ADMIN_PASSWORD=

# ===========================================
# App Settings — local SQLite (no cloud DB)
# ===========================================
SECRET_KEY=change-this-secret-key-in-production-use-long-random-string
DATABASE_URL=sqlite:///./data/facebook_posting.db
API_PORT=8002
CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174

# ===========================================
# SMTP — Backend only (NOT shown in dashboard)
# Gmail: enable 2FA, create App Password
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Facebook Posting
SMTP_USE_TLS=true

# Facebook — manual login in browser; session saved to data/facebook_session.json
# Required for scheduled posting app (wait loop, no duplicate 60s publish job):
STOP_AFTER_MARKETPLACE=true
# BROWSER_LOCALE=it-IT
